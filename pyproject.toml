[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "binhound"
version = "1.0.0"
description = "Binary analysis library for Termux, Linux, macOS, Windows"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.6"
